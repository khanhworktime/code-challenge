# Fancy Form

TechStack: Vite + React + Tailwinds + Vitest

To run this repo from root:

```termnial
cd ./src/problem2/fancy-form
bun install
bun dev
```

No env require

This repo has been deploy in: https://code-challenge-eight-mocha.vercel.app/

Notes:

- Wallet was connected with fake balances.
- The tokens on the giving api return multiple duplicate, I have been filter it to solve the duplicate.
- All other information like Network fee, Platform fee was fake.
